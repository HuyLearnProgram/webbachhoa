# Copy thành .env và điền mật khẩu DB thật (KHÔNG commit .env)
DB_URL=mysql+pymysql://root:YOUR_PASSWORD@localhost:3306/webnongsan

# Tuỳ chọn — mặc định xem app/config.py
# POOL_SIZE=80
# MMR_LAMBDA=0.7
# TIME_DECAY_TAU_H=72
# RETRAIN_CRON_HOUR=2
